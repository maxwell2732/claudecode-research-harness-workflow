{
  "_comment": "PostToolUse HTTP hook template for external metrics dashboards.",
  "_usage": "Add this entry to your hooks.json PostToolUse array to send tool usage events to a local metrics server.",
  "matcher": "Write|Edit|Bash|Task",
  "hooks": [
    {
      "type": "http",
      "url": "http://localhost:9090/metrics/hook",
      "timeout": 5,
      "headers": { "X-Source": "claude-code-harness", "X-Event": "PostToolUse" }
    }
  ]
}
